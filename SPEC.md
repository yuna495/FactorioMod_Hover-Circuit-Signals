# Hover Circuit Signals 仕様書

## 1. 概要

### 1.1 Mod名（仮称）

Hover Circuit Signals

### 1.2 目的

回路ネットワークへ接続されたエンティティにマウスカーソルを合わせた際、そのエンティティが受信・出力している回路信号を、画面右下の小型ウインドウへ自動表示する。

電柱を追加設置したり、専用の監視装置へ接続したりせず、コンビネータ、チェスト、インサータなどの信号を即座に確認できることを目的とする。

### 1.3 対象バージョン

- Factorio 2.0以降
- Space Ageの有無を問わない
- シングルプレイおよびマルチプレイ対応

## 2. 基本動作

1. プレイヤーがエンティティにマウスカーソルを合わせる。
2. 対象エンティティに回路接続端子があるか確認する。
3. 接続されている赤線・緑線の回路ネットワークを取得する。
4. 現在流れている信号を画面右下の独自ウインドウへ表示する。
5. マウスオーバー対象が変わった場合、表示内容を即座に切り替える。
6. 対象からカーソルを外した場合、ウインドウを非表示にする。

信号が変化するエンティティを見続けている間は、設定された更新間隔で数値を更新する。

---

## 3. 表示位置

### 3.1 基準点

画面右下を配置計算の基準とする。

ユーザーが指定する右方向・下方向のオフセットを画面右下から差し引き、算出された位置を、展開されるウインドウの左上座標とする。

```text
ウインドウ左上X = 画面幅  - 右方向オフセット
ウインドウ左上Y = 画面高さ - 下方向オフセット
```

オフセット値は「ウインドウの右端・下端までの余白」ではなく、画面右下の基準点からウインドウ左上までの距離として扱う。

### 3.2 初期値（暫定）

- 右方向オフセット：320px

- 下方向オフセット：220px

### 3.3 再配置

以下の場合に位置を再計算する。

- 画面解像度の変更

- UIスケールの変更

- オフセット設定の変更

- 表示モードの変更

### 3.4 画面外への配置防止

計算結果が画面外になる場合は、ウインドウ全体が画面内に収まる位置へ補正する。

## 4\. ウインドウデザイン

### 4.1 基本方針

- Factorio純正GUIに近い外観とする。

- バニラのGUIスタイル、余白、フォント、スロット背景を可能な限り流用する。
- 独自色や派手な装飾は使用しない。
- タイトルバーは設けず、表示領域を最小限にする。
- ウインドウは原則として情報表示専用とする。
- 背景はFactorio標準GUIに近い濃色フレームとする。
- 8. 回路信号を純正風の信号タイルで表示する。

### 4.2 基本構成

```txt
┌──────────────────────────┐
│ ● 赤  #184              │
│ [鉄板 -8k] [銅板 -4k] …    │
│ ● 緑  #205              │
│ [石炭 12k] [T 3]          │
└──────────────────────────┘
```

表示要素は以下の通り。

- 回路網見出し
- 赤線または緑線の識別
- 必要に応じて回路ネットワークID
- 信号スロット一覧
- 必要な場合のみ入力・出力見出し

## 5\. 信号表示

### 5.1 列数

- 1行あたり5列を基本とする。

- 各セルは「信号アイコン＋数値」で構成する。

- 6件目以降は次の行へ折り返す。

- 画面幅またはUIスケールの都合で5列を維持できない場合は、自動的に列数を減らしてもよい。

- 列数はユーザー設定で変更可能とする。

### 5.2 信号セル

各信号セルには以下を表示する。

- アイテム、液体、仮想シグナルなどの純正アイコン
- 現在値
- 品質が存在する場合は純正の品質表示
- マウスオーバー時の純正形式ツールチップ

### 5.3 数値表示

初期状態では、大きな数値を省略表示する。

タイル上では、Factorioに馴染む省略表記を使用する。

|  実際の値 | 表示例 |
| --------: | -----: |
|       999 |  `999` |
|     1,200 | `1.2k` |
|     1,999 |   `2k` |
|    15,000 |  `15k` |
| 2,400,000 | `2.4M` |
|    -8,000 |  `-8k` |

省略表示の有無はユーザー設定で変更可能とする。
Factorio純正の数値書式を利用できる場合は、それを優先する。
独自実装する場合は、表示領域に収まるよう丸める。小数点以下は省略する。

### 5.4 色の識別

- 赤線：純正の赤い回路線を想起させる小型マーカー
- 緑線：純正の緑の回路線を想起させる小型マーカー
- 色だけに依存せず、「赤」「緑」の文字または回路アイコンを併記可能とする

---

## 6\. 回路端子の扱い

### 6.1 単一端子エンティティ

チェスト、駅、インサータなど、通常の接続端子だけを持つ場合は以下を表示する。

- 赤線
- 緑線

この場合、「入力」「出力」という見出しは表示しない。

### 6.2 入出力端子を持つエンティティ

算術回路、判断回路など、入力端子と出力端子を区別できる場合は以下の順で表示する。

- 入力・赤
  - 緑
- 出力・赤
  - 緑

接続または信号が存在しない区画は省略する。

表示例：

```
入力
 ●赤  [鉄板]20k [銅板]5k
 ●緑  [鉄板]-10k

出力
 ●赤  [鉄板]10k
```

### 6.3 定数回路

定数回路には通常の意味での入力端子がないため、出力信号だけを表示する。

### 6.4 重複回路網

同じ回路ネットワークを複数端子から参照できる場合は、ネットワークIDと端子種別を基に重複を判定する。
同じネットワークを無条件に複数回表示しない。ただし、入力端子と出力端子で意味が異なる場合は別区画として表示する。

---

## 7\. 表示対象

### 7.1 対応対象

回路接続端子を取得できるエンティティを原則として自動対応させる。
例：

- 電柱
- チェストおよび物流チェスト
- インサータ
- ベルト
- 地下ベルト
- 分配器
- ポンプ
- タンク
- 列車駅
- ランプ
- プログラマブルスピーカー
- 算術回路
- 判断回路
- 定数回路
- Mod追加エンティティ

### 7.2 表示しない条件

以下の場合はウインドウを表示しない。

- マウスオーバー対象が存在しない
- 対象が無効または削除済み
- 対象が回路端子を持たない
- 赤線・緑線のどちらにも接続されていない
- Modがユーザー設定またはホットキーで無効化されている

### 7.3 信号が存在しない場合

回路線には接続されているが、流れている信号が0件の場合は、設定に応じて以下のどちらかとする。

- ウインドウを非表示にする
- 「信号なし」と1行だけ表示する

---

## 8\. 並び順と表示上限

### 8.1 並び順

ユーザーが以下から選択できるようにする。

- Factorio内部の信号順
- 信号名順
- 数値の絶対値が大きい順
- 正値を先、負値を後

初期値は「Factorio内部の信号順」とする。

### 8.2 表示上限

- 初期値：25信号
- 5列×5行を基本とする
- ユーザー設定で変更可能
- 上限を超えた場合は末尾に `+残り件数` を表示する
- 初期版ではスクロールを採用しない
- ウインドウのコンパクトさを優先する

表示例：

```
[鉄板]10k [銅板]5k [石炭]8k [石]2k [鋼材]1k
[歯車]500 [基板]300 [電池]200 [硫黄]100 [+12]
```

## 9\. ユーザー設定

設定種別は原則として `runtime-per-user` とする。

| 設定                      | 初期値         |
| ------------------------- | -------------- |
| Mod表示有効               | 有効           |
| 右方向オフセット          | 320px          |
| 下方向オフセット          | 420px          |
| 1行の列数                 | 5              |
| 最大表示信号数            | 25             |
| 更新間隔                  | 10tick         |
| 0件時に「信号なし」を表示 | 無効           |
| 回路ネットワークIDを表示  | 有効           |
| 信号並び順                | Factorio内部順 |

設定値には、画面外への配置や極端な更新負荷を防ぐ範囲制限を設ける。

## 10\. ホットキー

表示の有効・無効を即座に切り替えるホットキーを追加する。

- 仮初期キー：`Ctrl + Shift + C`
- キー設定画面から変更可能
- 切替状態はプレイヤーごとに保持する
- 切替時には簡潔なメッセージを表示する

表示例：

```
Hover Circuit Signals：表示オン
Hover Circuit Signals：表示オフ
```

## 11\. 更新処理

### 11.1 マウスオーバー対象の変更

マウスオーバー対象が変わった時、以下を行う。

- 前の対象への参照を破棄
- 新しい対象の端子構成を取得
- GUI構造を再生成
- 初回の信号を表示

### 11.2 数値更新

- 同じ対象を見続けている間だけ定期更新する。
- 初期更新間隔は10tickとする。
- 信号構成が変わらず数値だけ変化した場合は、GUI全体を作り直さず数値ラベルだけを更新する。
- 信号の種類が増減した場合のみ、信号一覧を再生成する。
- プレイヤーが表示を無効化している場合は更新しない。
- 全エンティティの常時走査は行わない。

### 11.3 マルチプレイ

- マウスオーバー対象はプレイヤー単位で管理する。
- GUIはプレイヤー単位で生成する。
- 表示設定もプレイヤー単位とする。
- ゲーム状態を変更せず、回路信号の読み取りだけを行う。
- 他プレイヤーの表示状態や対象に干渉しない。

## 12\. 負荷対策

- マウスオーバー中の一つのエンティティだけを監視する。
- 回路信号の取得は設定された更新間隔でのみ行う。
- GUIを非表示にしているプレイヤーは処理対象外とする。
- 回路未接続エンティティでは定期更新を開始しない。
- 同一ネットワークを複数回取得しない。
- GUI要素を毎回破棄・再生成せず、可能な限り再利用する。
- プレイヤーごとの処理を分散し、一tickに集中させないことを検討する。

## 13\. 技術的制約

- Factorio標準の右側エンティティ情報欄へ、Modから直接行を追加することはできない。
- 本Modは `player.gui.screen` 上に独自GUIを作成し、標準情報欄に近い外観を再現する。
- 標準情報欄の実際の高さや展開状態は取得できない。
- 標準情報欄の直下へ自動的に密着させることは保証しない。
- 表示位置は右下基準のユーザー指定オフセットで解決する。
- Mod追加エンティティの特殊な回路端子は、Factorio APIから取得可能な範囲のみ対応する。
- 同一エンティティが特殊な内部回路を使用している場合、実際の内部値すべてを取得できるとは限らない。

## 14\. 初期版の実装範囲

### 14.1 必須機能

- マウスオーバー対象の検出
- 赤線・緑線の信号取得
- 入力・出力端子の識別
- 右下基準の位置指定
- ウインドウ左上をオフセット座標として配置
- 5列表示
- Factorio純正風GUI
- 最大25信号
- 10tick間隔での更新
- ユーザー別設定
- ホットキーによる表示切替
- マルチプレイ対応
- 画面外への配置防止

### 14.2 初期版では見送る機能

- 標準エンティティ情報欄への直接統合
- ウインドウのドラッグ移動
- 信号履歴グラフ
- 特定信号のピン留め
- 遠隔地にある回路網の常時監視
- 信号検索
- 信号フィルター
- 回路網の編集
- 信号値のコピー操作
- 他エンティティとの比較表示

## 15\. 将来追加候補

- Altモード中のみ表示
- 特定キーを押している間だけ表示
- 入力値・出力値・差分値の強調
- 正数・負数の色分け
- 特定信号のピン留め
- 回路ネットワークIDのコピー
- 同じネットワークに属するエンティティのハイライト
- GUI上から更新を一時停止して値を比較する機能
- 最大値・最小値の記録
- 直前に確認したエンティティとの比較
- 信号の完全な数値をツールチップへ表示

## 16\. 完了条件

初期版は、以下をすべて満たした時点で完成とする。

1. 回路接続された主要なバニラエンティティで信号を表示できる。
2. 赤線・緑線を正しく区別できる。
3. 算術回路・判断回路の入力と出力を区別できる。
4. 信号を5列でコンパクトに折り返して表示できる。
5. 画面右下を基準として配置できる。
6. オフセット位置がウインドウ左上へ適用される。
7. オフセット値をユーザーが変更できる。
8. 解像度・UIスケール変更後も画面内へ表示できる。
9. マウスオーバーを外した際に表示が残らない。
10. 複数プレイヤーが異なる対象を見ても表示が干渉しない。
11. 信号が頻繁に変化しても顕著なUPS低下を起こさない。
12. GUIがFactorio純正画面から大きく浮かない。
