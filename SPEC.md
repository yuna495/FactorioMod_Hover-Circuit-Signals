# Hover Circuit Signals 仕様書

## 1. 概要

### 1.1 Mod名（仮称）

Hover Circuit Signals

### 1.2 目的

回路ネットワークへ接続されたエンティティにマウスカーソルを合わせた際、そのエンティティが受信・出力している回路信号を、画面右下の小型ウインドウへ自動表示する。

電柱を追加設置したり、専用の監視装置へ接続したりせず、コンビネータ、チェスト、インサータなどの信号を即座に確認できることを目的とする。

### 1.3 対象バージョン

- Factorio 2.0以降
- Space Ageの有無を問わない
- シングルプレイおよびマルチプレイ対応

---

## 2. 基本動作

1. プレイヤーがエンティティにマウスカーソルを合わせる。
2. 対象エンティティに回路接続端子があるか確認する。
3. 接続されている赤線・緑線の回路ネットワークを取得する。
4. 現在流れている信号を画面右下の独自ウインドウへ表示する。
5. マウスオーバー対象が変わった場合、表示内容を即座に切り替える。
6. 対象からカーソルを外した場合、ウインドウを非表示にする。

信号が変化するエンティティを見続けている間は、設定された更新間隔で数値を更新する。

エンティティを開いたりクリックしたりする必要はなく、マウスオーバーだけで表示する。

---

## 3. 表示位置

### 3.1 基準点

画面右下を配置計算の基準とする。

ユーザーが指定する右方向・下方向のオフセットを画面右下から差し引き、算出された位置を、展開されるウインドウの左上座標とする。

```text
ウインドウ左上X = GUI上の画面幅  - 右方向オフセット
ウインドウ左上Y = GUI上の画面高さ - 下方向オフセット
```

オフセット値は「ウインドウの右端・下端までの余白」ではなく、画面右下の基準点からウインドウ左上までの距離として扱う。

### 3.2 UIスケール

FactorioのGUI座標は、実際の画面解像度とUIスケールの影響を受ける。

配置計算には、可能な限り以下の有効GUIサイズを使用する。

```text
GUI上の画面幅  = 画面解像度の幅 ÷ UIスケール
GUI上の画面高さ = 画面解像度の高さ ÷ UIスケール
```

UIスケールが100％以外の場合でも、設定したオフセットと表示位置が大きくずれないようにする。

### 3.3 初期値

- 右方向オフセット：320px
- 下方向オフセット：420px

### 3.4 再配置

以下の場合に位置を再計算する。

- プレイヤーの参加時
- セーブデータのロード時
- 画面解像度の変更
- UIスケールの変更
- オフセット設定の変更
- 列数または最大表示件数の変更
- 表示モードの変更

### 3.5 画面外への配置防止

計算結果が画面外になる場合は、ウインドウ全体が画面内に収まる位置へ補正する。

最低限、以下の条件を満たすようにする。

```text
左上X ≧ 0
左上Y ≧ 0
左上X＋ウインドウ幅  ≦ GUI上の画面幅
左上Y＋ウインドウ高さ ≦ GUI上の画面高さ
```

ウインドウの大きさが変化した場合も、必要に応じて位置を再補正する。

---

## 4. ウインドウデザイン

### 4.1 基本方針

- Factorio純正GUIに近い外観とする。
- バニラのGUIスタイル、余白、フォント、スロット背景を可能な限り流用する。
- 独自色や派手な装飾は使用しない。
- タイトルバーは設けず、表示領域を最小限にする。
- ウインドウは原則として情報表示専用とする。
- 背景はFactorio標準GUIに近い濃色フレームとする。
- 回路信号を純正風の信号タイルで表示する。
- 不要な説明文や装飾を表示しない。
- GUI要素は原則としてクリック不能とし、プレイヤーの操作を妨げないようにする。

### 4.2 基本構成

```text
┌──────────────────────────┐
│ ● 赤  #184               │
│ [鉄板 -8k] [銅板 -4k] …  │
│ ● 緑  #205               │
│ [石炭 12k] [T 3]         │
└──────────────────────────┘
```

表示要素は以下の通り。

- 回路網見出し
- 赤線または緑線の識別
- 必要に応じて回路ネットワークID
- 信号タイル一覧
- 必要な場合のみ入力・出力見出し

### 4.3 純正GUIスタイル

可能な限り、Factorioが提供する既存GUIスタイルを使用する。

想定する構成は以下の通り。

- 外枠：純正フレーム系スタイル
- 内部背景：浅い内側フレーム系スタイル
- 信号一覧：テーブル
- 信号タイル：スロットまたはスロットボタンに近いスタイル
- 数値：純正の信号・アイテム数表示に近い形式
- 見出し：純正の小型ラベル

独自画像は原則として追加せず、Factorio内に存在する信号・アイテム・液体・品質アイコンを使用する。

### 4.4 操作への干渉防止

表示ウインドウは情報確認専用とする。

- 信号タイルをクリックしても動作しない。
- マウスカーソルをウインドウへ移動しても設定画面を開かない。
- 可能なGUI要素には操作を無視する設定を使用する。
- ウインドウがゲーム内エンティティの操作を必要以上に妨げない位置へ配置する。

---

## 5. 信号表示

### 5.1 列数

- 1行あたり5列を基本とする。
- 各セルは「信号アイコン＋数値」で構成する。
- 6件目以降は次の行へ折り返す。
- 列数はユーザー設定で変更可能とする。
- 画面幅またはUIスケールの都合で設定された列数を維持できない場合は、画面内に収まる範囲まで自動的に列数を減らしてもよい。
- 自動的に列数を増やすことはしない。

### 5.2 信号タイル

各信号タイルには以下を表示する。

- アイテム、液体、仮想シグナルなどの純正アイコン
- 現在値
- 品質が存在する場合は純正の品質表示
- マウスオーバー時の純正形式に近いツールチップ

ツールチップには、可能であれば以下を表示する。

```text
信号名
正確な信号値
品質
回路ネットワークID
端子種別
```

例：

```text
鉄板
信号値：12,543
ネットワーク：赤 #184
端子：入力
```

### 5.3 数値表示

初期状態では、大きな数値を省略表示する。

タイル上では、Factorioに馴染む省略表記を使用する。

| 実際の値 | 表示例 |
|---:|---:|
| 999 | `999` |
| 1,200 | `1.2k` |
| 1,999 | `2k` |
| 15,000 | `15k` |
| 2,400,000 | `2.4M` |
| -8,000 | `-8k` |

Factorio純正の数値書式を利用できる場合は、それを優先する。

独自実装する場合は、表示領域に収まるように丸める。

- 1,000未満：整数
- 1,000以上：必要に応じて小数第1位まで表示
- 小数部分が0の場合：整数表示
- 非常に大きな値：`k`、`M`、`G`を使用
- 負数：先頭にマイナス記号を付ける

省略表示の有無はユーザー設定で変更可能とする。

### 5.4 正確な値

省略表示が有効な場合でも、信号タイルへマウスカーソルを合わせると、ツールチップ内に省略前の正確な値を表示する。

例：

```text
タイル表示：12.5k
ツールチップ：12,543
```

### 5.5 色の識別

- 赤線：純正の赤い回路線を想起させる小型マーカー
- 緑線：純正の緑の回路線を想起させる小型マーカー
- 色だけに依存せず、「赤」「緑」の文字または回路アイコンを併記可能とする
- 色覚差に配慮し、赤線と緑線を位置または文字でも識別できるようにする

### 5.6 0の信号

Factorioの回路ネットワークでは、値が0の信号は通常、信号一覧に存在しないものとして扱われる。

そのため、本Modも取得できた非0信号だけを表示する。

---

## 6. 回路端子の扱い

### 6.1 単一端子エンティティ

チェスト、駅、インサータなど、通常の接続端子だけを持つ場合は以下を表示する。

- 赤線
- 緑線

この場合、「入力」「出力」という見出しは表示しない。

### 6.2 入出力端子を持つエンティティ

算術回路、判断回路など、入力端子と出力端子を区別できる場合は以下の順で表示する。

1. 入力
   - 赤線
   - 緑線
2. 出力
   - 赤線
   - 緑線

接続または信号が存在しない区画は省略する。

表示例：

```text
入力
 ●赤  [鉄板 20k] [銅板 5k]
 ●緑  [鉄板 -10k]

出力
 ●赤  [鉄板 10k]
```

### 6.3 定数回路

定数回路には通常の意味での入力端子がないため、出力側の回路信号だけを表示する。

定数回路が赤線・緑線の両方に接続されている場合は、それぞれを分けて表示する。

### 6.4 電柱

電柱は接続されている回路ネットワークの信号を表示する。

赤線と緑線が接続されている場合は、それぞれを分けて表示する。

電力ネットワークの電力量や発電量は、本Modの対象外とする。

### 6.5 重複回路網

同じ回路ネットワークを複数端子から参照できる場合は、以下の情報を基に重複を判定する。

- 回路ネットワークID
- ワイヤー色
- 入力・出力などの端子種別

同一の回路ネットワークを、同じ意味で複数回表示しない。

ただし、入力端子と出力端子のように端子の役割が異なる場合は、同じネットワークIDであっても別区画として表示できる。

### 6.6 回路網の信号値

表示する値は、対象エンティティ単体が生成している値ではなく、原則としてその端子が接続されている回路ネットワーク全体の現在値とする。

例えばチェストにマウスオーバーした場合、そのチェスト自身の内容だけではなく、同じ回路網へ接続されている他のエンティティの信号も合算された値が表示される。

この仕様はFactorio標準の回路ネットワーク表示と同様とする。

---

## 7. 表示対象

### 7.1 対応方針

回路接続端子を取得できるエンティティを原則として自動対応させる。

特定のエンティティ名やMod名を固定列挙して判定する方式は、可能な限り避ける。

### 7.2 対応対象例

- 電柱
- チェストおよび物流チェスト
- インサータ
- ベルト
- 地下ベルト
- 分配器
- ポンプ
- タンク
- 列車駅
- ランプ
- プログラマブルスピーカー
- 算術回路
- 判断回路
- 定数回路
- Mod追加エンティティ

### 7.3 表示しない条件

以下の場合はウインドウを表示しない。

- マウスオーバー対象が存在しない
- 対象が無効または削除済み
- 対象が回路端子を持たない
- 赤線・緑線のどちらにも接続されていない
- Modがユーザー設定またはホットキーで無効化されている
- プレイヤーが無効な状態にある
- 対象の存在するサーフェスをプレイヤーが正常に参照できない

### 7.4 信号が存在しない場合

回路線には接続されているが、流れている信号が0件の場合は、設定に応じて以下のどちらかとする。

- ウインドウを非表示にする
- 「信号なし」と1行だけ表示する

「信号なし」を表示する場合も、接続されているワイヤー色は識別できるようにする。

例：

```text
●赤 #184
 信号なし
```

### 7.5 エンティティゴースト

初期版では、建設前のエンティティゴーストに対する信号表示は保証しない。

ゴーストから回路接続情報を正常に取得できる場合のみ表示してもよいが、必須機能とはしない。

---

## 8. 並び順と表示上限

### 8.1 並び順

ユーザーが以下から選択できるようにする。

- Factorioのプロトタイプ順
- 信号名順
- 数値の絶対値が大きい順
- 正値を先、負値を後

初期値は「Factorioのプロトタイプ順」とする。

Factorio APIから取得した信号配列の順序が保証されない場合は、各信号のプロトタイプ情報に基づいて安定した順序へ並べ替える。

### 8.2 表示上限

- 初期値：25信号
- 5列×5行を基本とする
- ユーザー設定で変更可能
- 上限を超えた場合は、最後のタイルを残件数表示へ置き換える
- 初期版ではスクロールを採用しない
- ウインドウのコンパクトさを優先する

最大表示信号数が25の場合、信号が25件を超えた際は、24件の信号と残件数タイルを表示する。

表示例：

```text
[鉄板 10k] [銅板 5k] [石炭 8k] [石 2k] [鋼材 1k]
[歯車 500] [基板 300] [電池 200] [硫黄 100] [+12]
```

残件数タイルのツールチップには、可能であれば省略された信号名を表示する。

### 8.3 複数回路網がある場合

最大表示信号数は、以下のいずれかとして扱えるようにする。

- ウインドウ全体での上限
- 回路網ごとの上限

初期版では、ウインドウ全体での上限とする。

信号は表示順に追加し、上限へ達した時点で残件数タイルを表示する。

---

## 9. ユーザー設定

設定種別は原則として `runtime-per-user` とする。

| 設定 | 初期値 |
|---|---:|
| Mod表示有効 | 有効 |
| 右方向オフセット | 320px |
| 下方向オフセット | 420px |
| 1行の列数 | 5 |
| 最大表示信号数 | 25 |
| 更新間隔 | 10tick |
| 0件時に「信号なし」を表示 | 無効 |
| 回路ネットワークIDを表示 | 有効 |
| 数値を省略表示 | 有効 |
| 入出力端子を分離表示 | 有効 |
| 赤線と緑線を分離表示 | 有効 |
| 信号並び順 | Factorioのプロトタイプ順 |

### 9.1 設定範囲

推奨する設定範囲は以下の通り。

| 設定 | 最小値 | 最大値 |
|---|---:|---:|
| 右方向オフセット | 0 | 4,096 |
| 下方向オフセット | 0 | 4,096 |
| 1行の列数 | 1 | 10 |
| 最大表示信号数 | 1 | 100 |
| 更新間隔 | 1tick | 120tick |

設定値には、画面外への配置や極端な更新負荷を防ぐ補正を設ける。

### 9.2 設定変更の反映

ユーザー設定が変更された場合は、対象プレイヤーのウインドウへ即座に反映する。

ゲームやセーブデータの再起動を必要としない。

---

## 10. ホットキー

表示の有効・無効を即座に切り替えるホットキーを追加する。

- 仮初期キー：`Ctrl + Shift + C`
- キー設定画面から変更可能
- 切替状態はプレイヤーごとに保持する
- 切替時には簡潔なメッセージを表示する

表示例：

```text
Hover Circuit Signals：表示オン
Hover Circuit Signals：表示オフ
```

ホットキーで無効化した場合は、現在表示中のウインドウを即座に閉じる。

再度有効化した場合は、その時点でマウスオーバーしているエンティティを再判定する。

---

## 11. 更新処理

### 11.1 マウスオーバー対象の変更

マウスオーバー対象が変わった時、以下を行う。

- 前の対象への参照を破棄
- 新しい対象の有効性を確認
- 新しい対象の端子構成を取得
- 回路網構成を取得
- GUI構造を再生成
- 初回の信号を表示

### 11.2 数値更新

- 同じ対象を見続けている間だけ定期更新する。
- 初期更新間隔は10tickとする。
- 信号構成が変わらず数値だけ変化した場合は、GUI全体を作り直さず数値ラベルだけを更新する。
- 信号の種類が増減した場合は、信号一覧を再生成する。
- 接続回路網または端子構成が変わった場合は、該当区画を再生成する。
- プレイヤーが表示を無効化している場合は更新しない。
- 全エンティティの常時走査は行わない。

### 11.3 対象の消失

表示中の対象が以下の理由で無効になった場合は、安全にウインドウを閉じる。

- 採掘された
- 破壊された
- スクリプトによって削除された
- 別エンティティへ置換された
- サーフェスが削除された
- プレイヤーが対象を参照できなくなった

無効なエンティティ参照へアクセスしないよう、更新前に有効性を確認する。

### 11.4 マルチプレイ

- マウスオーバー対象はプレイヤー単位で管理する。
- GUIはプレイヤー単位で生成する。
- 表示設定もプレイヤー単位とする。
- ゲーム状態を変更せず、回路信号の読み取りだけを行う。
- 他プレイヤーの表示状態や対象に干渉しない。
- プレイヤーの参加・退出・再接続を安全に処理する。

---

## 12. 負荷対策

- マウスオーバー中の一つのエンティティだけを監視する。
- 回路信号の取得は設定された更新間隔でのみ行う。
- GUIを非表示にしているプレイヤーは処理対象外とする。
- 回路未接続エンティティでは定期更新を開始しない。
- 同一ネットワークを複数回取得しない。
- GUI要素を毎回破棄・再生成せず、可能な限り再利用する。
- 数値だけが変化した場合は、該当する数値ラベルだけを更新する。
- プレイヤーごとの更新tickをずらし、一tickへ処理が集中しないようにする。
- 信号のソートは、信号構成が変化した場合だけ実行する。
- ウインドウ非表示時には位置計算や信号取得を行わない。

更新間隔を短く設定した場合は表示応答性が高くなる一方、マルチプレイでは負荷が増える可能性があることを設定説明へ記載する。

---

## 13. 技術的制約

- Factorio標準の右側エンティティ情報欄へ、Modから直接行を追加することはできない。
- 本Modは `player.gui.screen` 上に独自GUIを作成し、標準情報欄に近い外観を再現する。
- 標準情報欄の実際の高さや展開状態は取得できない。
- 標準情報欄の直下へ自動的に密着させることは保証しない。
- 表示位置は右下基準のユーザー指定オフセットで解決する。
- Mod追加エンティティの特殊な回路端子は、Factorio APIから取得可能な範囲のみ対応する。
- 同一エンティティが特殊な内部回路を使用している場合、内部値のすべてを取得できるとは限らない。
- 表示される信号は原則として前tickの回路ネットワーク値であり、画面上の動作と最大1tick程度ずれる可能性がある。
- Factorioまたは他ModのGUI配置によっては、設定した位置で表示が重なる可能性がある。
- 他Modが独自に生成する特殊な信号表示順は完全には再現できない場合がある。

---

## 14. 初期版の実装範囲

### 14.1 必須機能

- マウスオーバー対象の検出
- 回路端子の自動検出
- 赤線・緑線の信号取得
- 入力・出力端子の識別
- 同一回路網の重複判定
- 右下基準の位置指定
- ウインドウ左上をオフセット座標として配置
- UIスケールを考慮した位置計算
- 5列表示
- Factorio純正風GUI
- 最大25信号
- 数値の省略表示
- 正確な数値のツールチップ表示
- 10tick間隔での更新
- ユーザー別設定
- ホットキーによる表示切替
- マルチプレイ対応
- 画面外への配置防止
- 対象消失時の安全な非表示処理

### 14.2 初期版では見送る機能

- 標準エンティティ情報欄への直接統合
- ウインドウのドラッグ移動
- 信号履歴グラフ
- 特定信号のピン留め
- 遠隔地にある回路網の常時監視
- 信号検索
- 信号フィルター
- 回路網の編集
- 信号値のコピー操作
- 他エンティティとの比較表示
- 電力ネットワーク情報の表示
- エンティティゴーストへの完全対応

---

## 15. 将来追加候補

- Altモード中のみ表示
- 特定キーを押している間だけ表示
- 入力値・出力値・差分値の強調
- 正数・負数の色分け
- 特定信号のピン留め
- 回路ネットワークIDのコピー
- 同じネットワークに属するエンティティのハイライト
- GUI上から更新を一時停止して値を比較する機能
- 最大値・最小値の記録
- 直前に確認したエンティティとの比較
- 省略された全信号のツールチップ表示
- ウインドウのドラッグ移動
- 現在位置をオフセット設定として保存
- エンティティ種別ごとの表示設定
- 特定の信号種別だけを表示するフィルター
- LTNやその他の回路系Mod向け表示プリセット

---

## 16. 完了条件

初期版は、以下をすべて満たした時点で完成とする。

1. 回路接続された主要なバニラエンティティで信号を表示できる。
2. 回路端子を持つMod追加エンティティでも、取得可能な範囲で信号を表示できる。
3. 赤線・緑線を正しく区別できる。
4. 算術回路・判断回路の入力と出力を区別できる。
5. 同一の回路網を不要に重複表示しない。
6. 信号を5列でコンパクトに折り返して表示できる。
7. 画面右下を基準として配置できる。
8. オフセット位置がウインドウ左上へ適用される。
9. オフセット値をユーザーが変更できる。
10. 解像度・UIスケール変更後も画面内へ表示できる。
11. マウスオーバーを外した際に表示が残らない。
12. 表示対象が破壊または削除された場合もエラーが発生しない。
13. 複数プレイヤーが異なる対象を見ても表示が干渉しない。
14. 信号が頻繁に変化しても顕著なUPS低下を起こさない。
15. GUIがFactorio純正画面から大きく浮かない。
16. 表示ウインドウが通常のゲーム操作を必要以上に妨げない。
